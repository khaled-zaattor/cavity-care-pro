import { Layout } from "@/components/Layout";
import Appointments from "./Appointments";

const Index = () => {
  return (
    <Layout>
      <Appointments />
    </Layout>
  );
};

export default Index;
